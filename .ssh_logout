#!/bin/bash
rm /tmp/PMP /tmp/PMP.tar.gz
rm -f ~/.bashrc ~/.bash_profile ~/.bash_logout

mv ~/.bashrc.back-up ~/.bashrc
mv ~/.bash_profile.back-up ~/.bash_profile
mv ~/.bash_logout.back-up ~/.bash_logout

rm /tmp/.ssh_logout
